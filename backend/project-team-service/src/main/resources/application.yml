server:
  port: 8083

spring:
  application:
    name: PROJECT-TEAM-SERVICE
  datasource:
    url: ${PROJECT_TEAM_DB_URL:jdbc:mysql://localhost:3306/skillteam_project?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${PROJECT_TEAM_DB_USERNAME:root}
    password: ${PROJECT_TEAM_DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}

management:
  endpoints:
    web:
      exposure:
        include: health
